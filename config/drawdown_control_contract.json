{
  "schema": "canli.alphac-drawdown-control-contract.v1",
  "version": "1.1",
  "author": "Arhan Canli",
  "status": "MECHANISM_LIVE_BOUND_ENFORCED_UP_TO_ONE_DAY_OVERSHOOT",
  "frozen_at": "2026-09-14",
  "frozen_note": "v1.1 parameters re-derived from the restated bound on 2026-09-14 (evening), before the v1.1 study run; v1.0 was frozen and measured earlier the same day.",
  "measurement": {
    "measured_on": "2026-09-14",
    "artifact": "artifacts/analysis/drawdown_control_v1/result.json",
    "public_path": "/glassbox/drawdown_control_v1.json",
    "result_summary": "Every figure lives in the artifact; this contract records only that the declared rule was applied once, after the parameters were frozen, and what it decided.",
    "accepted_as_bound_mechanism": true,
    "nothing_was_tuned_after_the_result": true,
    "conservative_p95_with_absorbing_ladder": 0.10067091639034838,
    "conservative_p99_with_absorbing_ladder": 0.10196252172481021
  },
  "protocol": "docs/design/DRAWDOWN_CONTROL_V1_PROTOCOL.md",
  "owner_goal": "Combined portfolio maximum drawdown no greater than 0.10 (owner restatement of 2026-09-14, config/owner_goals.json goals.combined_max_drawdown): a bound on REALIZED maximum drawdown rather than an expected-maximum-drawdown target, with realized, historical-scenario, simulated expected and simulated tail drawdowns published separately.",
  "bound": 0.1,
  "bound_source": "config/owner_goals.json goals.combined_max_drawdown.bound",
  "ladder": {
    "implementation": "alphaforge.risk.ladder_paths.simulate_book_ladder (research twin of alphaforge.risk.monitors.DrawdownLadder, asserted step-equal)",
    "measured_on": "book daily close equity versus its high-water mark",
    "dd_half_frac": 0.05,
    "dd_flat_frac": 0.1,
    "release_frac_of_half": 0.75,
    "flat_state": "ABSORBING_UNTIL_OWNER_REVIEW",
    "secondary_scenario_flat_cooldown_days": 14,
    "exposure_timing": "multiplier in force at the previous close scales the next day's book return",
    "derivation": "half level = bound / 2; flat level = bound; release = the live ladder's hysteresis; nothing fitted"
  },
  "evaluation": {
    "reuses_study": "docs/design/CURRENT_BOOK_DRAWDOWN_STUDY_PROTOCOL.md",
    "paths": 10000,
    "horizon_calendar_days": 730,
    "models": [
      "circular_moving_block_bootstrap_63",
      "correlation_regime_stress_0.50"
    ],
    "drift_conventions": [
      "zero_drift_as_published",
      "research_window_drift_added_back"
    ],
    "baseline_must_reproduce_published_study_within": 1e-12
  },
  "acceptance_rule": {
    "declared_before_measurement": true,
    "derivation": "The v1.0 rule scaled to the new bound by the same relative slack it declared: p95 within 9.09% of the bound and p99 within 18.18% of the bound (0.12 / 0.11 and 0.13 / 0.11). The slack is the one-day overshoot a close-to-close ladder cannot avoid. Derived, not chosen.",
    "conservative_model": "the worse of the two models per statistic",
    "p95_max_drawdown_with_absorbing_ladder_max": 0.109091,
    "p99_max_drawdown_with_absorbing_ladder_max": 0.118182,
    "return_cost_is_a_price_not_a_gate": true,
    "disclosure_scratch_cell_before_redeclaration": "A scratch cell at 5.0 / 10.0 was run at 18:30Z on 2026-09-14, before this rule was re-declared, to learn whether the bound was reachable at all (operator log). Its figures, conservative p95 0.1007 and p99 0.1020, were known when this rule was written. The rule above is v1.0's rule scaled to the bound, which fixes the thresholds at 0.1091 and 0.1182 without reference to those figures; the official run below is the one that decides."
  },
  "published_measures": {
    "realized_forward": "artifacts/engineering/forward_drawdown_evidence.json (paper record since 2026-08-07, descriptive only)",
    "historical_scenario": "artifacts/analysis/current_book_drawdown/result.json calibration.observed_book_max_drawdown (research window; the four sleeve curves share this window only on their common calendar days)",
    "simulated_expected": "artifacts/analysis/drawdown_control_v1/result.json, per model, with and without the ladder",
    "simulated_tail": "artifacts/analysis/drawdown_control_v1/result.json p95 and p99, per model, with and without the ladder"
  },
  "trial_accounting": {
    "hypothesis_identities_spent": 0,
    "return_data_opened": false,
    "classification": "EXISTING_RETURN_RISK_REMEASUREMENT"
  },
  "history": [
    {
      "version": "1.0",
      "frozen_at": "2026-09-14",
      "bound": 0.11,
      "ladder": {
        "dd_half_frac": 0.055,
        "dd_flat_frac": 0.11,
        "release_frac_of_half": 0.75
      },
      "acceptance_rule": {
        "p95_max_drawdown_with_absorbing_ladder_max": 0.12,
        "p99_max_drawdown_with_absorbing_ladder_max": 0.13
      },
      "measurement": {
        "measured_on": "2026-09-14",
        "artifact": "artifacts/analysis/drawdown_control_v1/result.json",
        "public_path": "/glassbox/drawdown_control_v1.json",
        "result_summary": "Every figure lives in the artifact; this contract records only that the declared rule was applied once, after the parameters were frozen, and what it decided.",
        "accepted_as_bound_mechanism": true,
        "nothing_was_tuned_after_the_result": true
      },
      "measured_result": "conservative p95 0.1102, p99 0.1116 with the absorbing ladder (regime model), halt probability 0.064, two-year drift cost 0.0074; accepted",
      "superseded_on": "2026-09-14",
      "superseded_because": "The owner restated the maximum-drawdown goal as 10 percent (config/owner_goals.json, in force 2026-09-14). A ladder that goes flat at 11 percent cannot enforce a 10 percent bound, so the ladder is re-derived from the new bound by the same rule and re-measured by the same study. Nothing about v1.0 was wrong for the 11 percent bound it served."
    }
  ],
  "activation": {
    "live": true,
    "one_switch": "This flag. Read at trade time by alphaforge.risk.book_ladder.BookLadderProvider (every sleeve) and hashed into the live-configuration fingerprint (scripts/export_live_config_fingerprint.py risk_path_settings.book_ladder_activation_live), so the public contract, the trading path and the live-change guard cannot disagree about whether the brake is on.",
    "requires": [
      "an owner decision recorded here as activation.decision, because a live-configuration fingerprint change starts a new forward-evidence epoch",
      "a live_change_contract.json change_log entry and re-pinned fingerprint",
      "each sleeve loop reading the book-level multiplier before sizing, with a restart-safe persisted ladder state (the crypto ladder's 2026-09-05 lesson)"
    ],
    "progress_2026_09_14": {
      "producer": "scripts/book_drawdown_ladder.py replays the combined book's published daily marks (data/paper/state.json live_curve) through this ladder every publish; artifact artifacts/engineering/book_drawdown_ladder.json (published as /glassbox/book_drawdown_ladder.json on both hosts) and consumer file var/book_ladder/current.json, bound by content hash. State is derived from the whole marked history each run, never stored.",
      "consumers_wired_default_off": [
        "scripts/live_cycle.py (equity sleeves): target weights multiplied by the reading; a multiplier of 0 becomes a flatten; one loud line per cycle; fail-open at 1.0 with the error printed",
        "alphaforge.portfolio.strategy.BlendStrategy(book_multiplier=...) (crypto loop via paper_cmds._build_loop): applied after the sleeve ladder; halts every bar at 0; rescales every bar below 1; pre-multiplier book persisted per cycle (strategy_last_targets) and restored on boot so a de-gross acts on the next hold bar under --once"
      ],
      "rearm_register": "config/book_ladder_rearms.json; a FLAT_HALTED book stays flat until a dated owner entry there, and the ladder restarts with the high-water mark reset to the first mark on or after that date",
      "health": "scripts/health_check.py C12-book-ladder: FAIL when the artifact or consumer file is missing or unbound, FAIL critical on FLAT_HALTED (owner rearm required), WARN at HALF_GROSS or when stale",
      "fingerprint": "coverage extended 2026-09-14 (live_change_contract.json change_log entry 7); activation false is the declared state"
    },
    "activation_procedure": [
      "1. Owner decision recorded here as activation.decision (date, reason, who).",
      "2. Set activation.live to true in this file; set BOOK_LEVEL_DRAWDOWN_LADDER in scripts/paper_trading_state.py to this ladder so the published aggregation policy stops declaring 'no book-level ladder' (tests/unit/test_live_change_is_declared.py pins that declaration).",
      "3. Re-run scripts/export_live_config_fingerprint.py; write the change_log entry in config/live_change_contract.json with contaminates_forward_record true and the activation date; re-pin declared_fingerprint and forward_evidence_contract.required_live_config_fingerprint; re-pin scripts/analyze_current_book_drawdown.py.",
      "4. rsync the checkout to Frankfurt so the crypto loop reads the contract with live true (source https, the public artifact); confirm cycle.book_ladder appears in its log with book_applied true.",
      "5. Mark the forward record: returns before and after the activation date are separate evidence epochs and are not pooled."
    ],
    "status_after_activation": "MECHANISM_LIVE_BOUND_ENFORCED_UP_TO_ONE_DAY_OVERSHOOT",
    "activated_on": "2026-09-15",
    "decision": {
      "recorded_at": "2026-09-14T20:46:56.027679+00:00",
      "by": "Arhan Canli (owner)",
      "words": "i give you full permision for the activiations so you can go ahead",
      "how": "scripts/activate_book_drawdown_brake.py"
    }
  },
  "claim_boundary": "This contract declares a risk policy and how it is measured. It does not establish a maximum drawdown for the live book, does not forecast returns, and changes no published performance figure. A brake bounds each drawdown episode up to a one-day overshoot; it does not make losses impossible."
}
