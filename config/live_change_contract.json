{
  "schema": "canli.alphac-live-change-contract.v1",
  "purpose": "A change to how the LIVE book trades must be DECLARED before it ships. This file is the declaration. It records the configuration currently in force, and every change to it, with the reason and the evidence. If the measured fingerprint does not match declared_fingerprint, the guard fails and the deploy is blocked.",
  "why": "The forward record is the only evidence that can ever defeat deflation: a pre-registered book run forward is N=1, while any selected backtest must carry the corrected 228-identity search context. That record is the most valuable asset in the project. A live configuration change silently contaminates it, and until this file existed nothing stood in the way of one -- there were 85 checks around admitting a sleeve and none around re-sizing the whole book.",
  "the_incident_this_exists_for": {
    "date": "2026-08-21",
    "what": "cov_halflife_bars (default 720) was changed to cov_halflife_days (default 21). No call site passes that argument, the walk-forwards are regenerated by the daily ticks, and live_cycle.py submits the last leg's weights to the broker -- so the change would have re-sized two broker-executed sleeves on the next tick. The commit said 'nothing was re-run'. Reverted the same day after an adversarial audit.",
    "the_rule_it_produced": "A DEFAULT NOBODY OVERRIDES IS NOT A DEFAULT, IT IS THE PRODUCTION SETTING. Before changing one, grep for who passes it; if nobody does, you are changing the live book."
  },
  "declared_fingerprint": "sha256:553aff517efdc069c2ee80747ef9a9d750da817ceeab8dbc344cae050b814e09",
  "declared_surface": {
    "strategy_settings": {
      "allocator": "rank",
      "rebalance_bars": 24,
      "rebalance_anchor": "run",
      "cov_window_bars": 720,
      "cov_halflife_days": null,
      "cov_min_periods": 240,
      "realized_vol_halflife_bars": 240,
      "cost_frac_oneway": 0.001
    },
    "risk_path_settings": {
      "book_ladder_activation_live": true,
      "book_ladder_dd_half_frac": 0.05,
      "book_ladder_dd_flat_frac": 0.1,
      "book_ladder_release_frac_of_half": 0.75,
      "book_ladder_source": "https",
      "book_ladder_max_age_days": 4,
      "vol_target_ann": 0.15,
      "vol_scale_max": 1.5,
      "gross_max": 1.0,
      "dd_halve_gross": -0.1,
      "dd_flat_halt": -0.15,
      "flat_cooldown_bars": 336,
      "staleness_max_bars": 2,
      "w_max": 0.15
    },
    "book_composition": {
      "sleeves": {
        "alphaforge": 0.25,
        "alphamax": 0.25,
        "managed_futures": 0.25,
        "alphavintage": 0.25
      },
      "strategic_tilt_pct": 0.1,
      "strategic_tilt_mix": {
        "BTC": 0.5,
        "SPY": 0.5
      }
    },
    "book_aggregation_settings": {
      "scheme": "fixed",
      "book_level_vol_target_ann": null,
      "book_level_drawdown_ladder": {
        "dd_half_frac": 0.05,
        "dd_flat_frac": 0.1,
        "release_frac_of_half": 0.75,
        "flat_state": "ABSORBING_UNTIL_OWNER_REARM",
        "applied_by": "every sleeve multiplies its target gross by the published book multiplier",
        "source": "config/drawdown_control_contract.json",
        "activated_on": "2026-09-15"
      },
      "missing_mark_policy": "ZERO_CONTRIBUTION_ON_MISSING_DAILY_SLEEVE_MARK",
      "strategic_overlay_is_book_vol_scaled": false,
      "claim_boundary": "Constituent strategies own their internal sizing. ALPHAC combines their realized returns at the committed fixed-weight schedule and adds the separately disclosed strategic overlay. It applies no second book-level volatility target. It does apply the declared book-level drawdown ladder (half gross at 5.0% below the high-water mark, flat at 10.0%, absorbing until an owner rearm), which every sleeve reads before sizing."
    }
  },
  "declared_at": "2026-09-15",
  "how_to_change_the_live_book": [
    "1. Measure the change on the LIVE path, not a simulation of it. The overlay halflife was priced against an untruncated EWMA the production estimator is not (artifacts/analysis/live_covariance_memory).",
    "2. Write the reason and the evidence path into change_log below.",
    "3. Re-run scripts/export_live_config_fingerprint.py and update declared_fingerprint.",
    "4. Expect the forward record to be marked at that date. A change is a new specification, and the record before it and after it are not the same experiment."
  ],
  "change_log": [
    {
      "date": "2026-08-21",
      "change": "initial declaration; configuration unchanged",
      "reason": "Establishes the baseline against which future changes are detected. The configuration recorded here is the one the book has been running since the v3 re-baseline on 2026-08-07.",
      "evidence": "artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false
    },
    {
      "date": "2026-08-21",
      "change": "fingerprint COVERAGE extended to the overlay, the drawdown ladder and the per-name clip (backlog A2). The live configuration itself is unchanged.",
      "reason": "The first fingerprint covered BlendStrategy's constructor defaults and the book composition, and claimed to cover 'how the live book trades'. It did not: the overlay's vol target and scale ceiling, the ladder's halve and flatten depths, the halt cooldown, the staleness limit and the inviolable per-name cap are read off Settings rather than passed to the constructor, so all eight sat outside the guard. Each one moves every position in the book. Read through Settings and PortfolioConstraints.from_settings so a config change reaches the fingerprint by the same path it reaches the book.",
      "evidence": "artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false,
      "note": "A coverage change, not a configuration change: every value recorded here is the value that was already running. The fingerprint hash necessarily moves because the surface it hashes is larger."
    },
    {
      "date": "2026-08-23",
      "change": "fingerprint COVERAGE extended to the ALPHAC flagship aggregation layer. The live configuration itself is unchanged.",
      "reason": "The prior surface recorded constituent BlendStrategy risk settings and the flagship composition but did not record how the constituent returns are combined. That omission allowed the constituents' 15% internal volatility target to be described incorrectly as a book-level target. ALPHAC actually compounds the committed fixed-weight sleeve schedule, adds a fixed unscaled strategic overlay, treats a missing daily sleeve mark as zero contribution, and applies no second book-level volatility target or drawdown ladder. These were already the running semantics; this declaration makes them explicit and mutation-guarded.",
      "evidence": "artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false,
      "note": "A coverage and claim-scope correction, not a trading change. The fingerprint moves because the previously omitted aggregation layer is now hashed."
    },
    {
      "date": "2026-09-05",
      "change": "two live-path defects fixed; the declared configuration is UNCHANGED and the fingerprint does not move. (1) SignalService now scopes its cross-section to the sleeve's asset class on both the research and the live path. It was the raw cross-asset universe membership, so the carry feature asked the instrument record for an equity's funding interval and the weekly rebalance raised KeyError on the trading host on 2026-08-13, 08-20, 08-27 and 09-03; the sleeve held its 2026-07-30 book for five weeks. (2) LiveLoop.recover_on_boot now replays the recorded equity curve through the DrawdownLadder. Under --once a fresh ladder was built every hour with HWM = current equity, so the declared 10% halve / 15% flatten brakes could never fire; on this sleeve the restored ladder enters HALF_GROSS (drawdown 10.9% from the recorded high-water mark, which it first crossed on 2026-08-26).",
      "reason": "Both are the declared configuration finally executing, not a new configuration: the blessed weekly carry rebalance and the declared drawdown ladder were in the contract and were not running. Fixing them changes how the live book trades from the next cycle on (the crypto sleeve resumes weekly rebalancing and sizes at half gross until its drawdown recovers below the release depth), so it is declared here before it ships. Measured on the live path: the scoped cross-section on the real lake at the crashed timestamp yields the 20 perpetuals and drops 2,088 equities; the ladder replay over the real equity_curve lands in HALF_GROSS. Neither threshold, weight, composition nor overlay parameter was edited.",
      "evidence": "tests/unit/test_signal_service.py::TestCrossSectionIsScopedToTheSleeve; tests/unit/test_live_loop.py::TestLadderRestoration; tests/unit/test_health_crypto_rebalance.py; the dated CORRECTION 2026-09-05 entry in scripts/paper_trading_state.py transparency_entries(); artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false,
      "note": "The contaminated window is BEFORE this change, not after it: from 2026-07-30 to the fix the crypto sleeve's marks are genuine but its positions were stale rather than chosen, and the ladder never acted. That window is disclosed in the transparency correction and stays in the record. A third finding is disclosed and NOT changed here: universe_refresher is never passed to LiveLoop in paper_cmds, so the monthly universe refresh has not run live (cross-section selected 2026-06-01). Re-wiring it changes the traded cross-section and is a separate declared change."
    },
    {
      "date": "2026-09-06",
      "change": "the live loop's monthly universe refresh is WIRED (paper_cmds._build_loop now passes a _UniverseRefresherAdapter over the same asset-class-scoped UniverseBuilder the `universe rebuild` CLI uses). Declared settings are unchanged and the fingerprint does not move; what changes is that the declared monthly universe rebalance, which the backtest always applied, now happens live. On the first cycle after deploy the stored crypto membership (newest rebalance 2026-06-01) is older than the current month, so the self-healing catch-up rebuilds it and the traded cross-section moves to the current month's top names; positions in exited names are flattened by the strategy's held-but-unsized rule.",
      "reason": "LiveLoop has carried the universe_refresher seam and its 2026-08-11 self-healing catch-up since they were written, and _build_loop never passed one, so both were dead code live and the crypto sleeve traded a cross-section selected on 2026-06-01 through September. A universe that never refreshes is not the strategy the evidence blessed. Measured on the wiring itself: an AST guard asserts LiveLoop(...) in _build_loop names universe_refresher with a non-None value, and unit tests pin that the adapter rebuilds over [backfill_start, cycle_ts] with now=cycle_ts and reports the newest rebalance for THIS asset class only, so a fresh equity rebalance in the shared store cannot mask a stale crypto membership.",
      "evidence": "tests/unit/test_live_universe_refresher_wiring.py; tests/unit/test_universe_rebalance_selfheal.py; artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false,
      "note": "The forward record from 2026-06-01 to this deploy ran on a universe that did not refresh; that is the already-disclosed defect window, not a new one. This change restores the declared cadence. The first refreshed cycle will trade turnover that a monthly-refreshing book would have spread across three rebalances; it is booked as it happens, not smoothed."
    },
    {
      "date": "2026-09-06",
      "change": "the realized-volatility leg of the sizing overlay is restored under the --once deployment. Each cycle now persists the overlay scale in force (BlendStrategy.last_scale) on its equity_curve row (additive nullable column overlay_scale), and recover_on_boot seeds the strategy's equity and scale histories from the recorded curve through the new BlendStrategy.seed_history seam. Declared thresholds and parameters are unchanged; the fingerprint does not move. What changes is that sigma_hat = max(ex_ante, realized) can now bind on the realized side live, as the backtest always allowed; at rebalances where the de-levered realized vol exceeds the ex-ante estimate the book will be sized smaller than it has been.",
      "reason": "The strategy kept both histories in memory and production runs one process per cycle, so every live rebalance saw one equity point and the realized leg was 0.0 by construction; the fast regime detector, measured to bind 81.8% of days when correctly scaled (artifacts/analysis/frontier_14), never bound live. The 2026-08 realized-leg scale correction was therefore correct, tested, and never executing. Measured on the path itself: a fresh strategy's _realized_vol_ann is 0.0 (pinned as a test), a seeded one is positive, and the loop test proves a second process receives the scales the first recorded, in order, with the documented t-1 alignment.",
      "evidence": "tests/unit/test_strategy_history_seed.py; tests/unit/test_store_overlay_scale.py; tests/unit/test_live_loop.py::TestStrategyHistoryRestoration; artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false,
      "note": "Rows recorded before the column existed carry no scale and are masked by the de-lever, so the leg warms up from the deploy forward rather than being back-filled from a guess. The window before this change ran with the realized leg inert; it is the same disclosed defect window as the ladder's."
    },
    {
      "date": "2026-09-14",
      "change": "fingerprint COVERAGE extended to the BOOK-level drawdown brake (drawdown control v1, config/drawdown_control_contract.json). The live configuration itself is unchanged: activation.live is false, no sleeve applies a multiplier other than 1.0, and every previously declared threshold and parameter is the same. The consumers are wired and default-off: scripts/live_cycle.py multiplies the equity sleeves' target weights by the published book multiplier (var/book_ladder/current.json, written every publish by scripts/book_drawdown_ladder.py from the combined book's published marks), and BlendStrategy applies it after its own sleeve ladder in the crypto loop (paper_cmds._build_loop passes a BookLadderProvider; a book halt flattens every bar, a book de-gross rescales every bar). The new constructor seam book_multiplier is excluded from strategy_settings as call-site wiring, like mu_provider; what it does is hashed in risk_path_settings as book_ladder_activation_live, the two depths, the release fraction and the read source. One live-path repair rides along, same class as the 2026-09-06 realized-leg entry: the pre-multiplier target book is now persisted per cycle (strategy_last_targets) and restored on boot, so under --once a sleeve-ladder or book de-gross acts on the next hold bar, as the backtest always did, instead of waiting for the next rebalance boundary.",
      "reason": "The owner's 11 percent maximum-drawdown bound is a bound on the COMBINED book and had no mechanism: each sleeve carries its own -10/-15 percent ladder and the declared aggregation layer applies no book-level ladder. Drawdown control v1 measured the declared 5.5/11 percent absorbing book ladder on the current-composition study paths (artifacts/analysis/drawdown_control_v1: regime p95 0.1645 to 0.1102, p99 0.2065 to 0.1116, halt probability 0.064) and accepted it as the bound mechanism. Activation is a separate, later declared change. Wiring the consumers behind a fingerprinted switch first means the flip itself is visible to this guard; and the fingerprint had already moved on the new constructor default in the 12:25Z tick of 2026-09-14, which this guard blocked, exactly as designed.",
      "evidence": "artifacts/engineering/live_config_fingerprint.json; artifacts/engineering/book_drawdown_ladder.json; tests/unit/test_book_ladder_provider.py; tests/unit/test_book_drawdown_ladder.py; tests/unit/test_live_cycle_execution.py (book ladder cases); tests/unit/test_live_loop.py (TestLastTargetsRestoration, TestBookLadderLogging); tests/unit/test_live_store.py; docs/design/DRAWDOWN_CONTROL_V1_PROTOCOL.md; docs/design/OPERATOR_LOG_2026-09.md",
      "contaminates_forward_record": false,
      "note": "A coverage extension, not a trading change. The fingerprint moves because the brake's switch, depths, release and read source are now hashed; with activation.live false the traded book is byte-identical. The persisted pre-multiplier book changes only WHEN a de-gross is in force, and none is."
    },
    {
      "date": "2026-09-14",
      "change": "the PIT adjusted-close kernel (src/alphaforge/features/library/equity_price.py adjusted_close) now DIVIDES pre-ex prices by the stored split factor instead of multiplying by it. Every lake stores the vendor factor, new shares per old (Polygon split_to/split_from, Sharadar ACTIONS.value; documented in data/schemas.py), while the kernel and its tests assumed old shares per new. Declared thresholds, defaults, weights and the fingerprint are unchanged; what changes is the input every equity price feature sees on names that split inside its lookback: eq_mom_252_21 (the only alpha of both live equity walk-forwards), reversal, realized volatility and beta.",
      "reason": "Measured on the production lakes before the fix (artifacts/audit/split_adjustment_direction.json): 4,135 of 4,662 determined splits in data/lake and 4,000 of 4,440 in data/lake_sharadar came out of the kernel with an adjusted ex-date jump of twice the raw one, the signature of an inverted factor; Apple's 2020 four-for-one adjusted to 1,996.92 the day before and 129.04 after, a fake 93.5 percent drop that a momentum rank reads as a loser. The 2026-08 repair that made splits apply at all applied them inverted; the alphamax beta-neutral probe disclosed it on 2026-09 and worked around it locally without touching the kernel. A cross-lake guard (tests/unit/test_split_adjustment_direction_guard.py) now checks the kernel against Apple's real split on both lakes so the two conventions can never again pass separately.",
      "evidence": "artifacts/audit/split_adjustment_direction.json; tests/unit/test_split_adjustment_direction_guard.py; tests/unit/test_factors_equity_price.py (fixtures on the vendor convention); scripts/probe_alphamax_betaneutral.py (its local inversion retired); docs/design/OPERATOR_LOG_2026-09.md",
      "contaminates_forward_record": false,
      "note": "A live-path repair, same class as the 2026-09-05 and 2026-09-06 entries: the declared configuration is unchanged and the fingerprint does not move. The walk-forwards the live cycles read regenerate on the next tick, so the equity target books change from that cycle on names that split within 252 sessions. Stored ratios that follow the reciprocal convention (about 5 percent of splits, listed by the audit) become wrong under the fixed kernel and are repaired separately, by a versioned lake correction with its own receipt."
    },
    {
      "date": "2026-09-14",
      "change": "drawdown control v1.1: the BOOK-level ladder in config/drawdown_control_contract.json is re-derived from the owner's restated 10 percent bound (half gross at 5.0 percent below the high-water mark, flat at 10.0 percent; was 5.5 / 11.0), so the hashed depths book_ladder_dd_half_frac and book_ladder_dd_flat_frac moved. The live configuration that trades is unchanged: activation.live is false, every sleeve still applies a book multiplier of exactly 1.0, and no other threshold or parameter moved.",
      "reason": "The owner restated the maximum-drawdown goal as 10 percent on 2026-09-14 (config/owner_goals.json). A ladder that goes flat at 11 percent cannot enforce a 10 percent bound. v1.1 re-derives every ladder parameter from the bound by the same rule (half = bound / 2, flat = bound) and re-measures it by the same protocol, seeds, paths and models; v1.0 is kept as history in the contract. Declaring the ladder before activation keeps the later flip a one-switch change that this guard sees.",
      "evidence": "config/drawdown_control_contract.json (version 1.1, history[0] = v1.0); docs/design/DRAWDOWN_CONTROL_V1_PROTOCOL.md (v1.1 section); artifacts/analysis/drawdown_control_v1/result.json (the v1.1 measurement); artifacts/engineering/live_config_fingerprint.json; docs/design/OPERATOR_LOG_2026-09.md",
      "contaminates_forward_record": false,
      "note": "Not a trading change: the brake is not activated and sizing is identical before and after this commit. The forward-evidence epoch does not restart. When the brake is activated, that activation is its own declared change (scripts/activate_book_drawdown_brake.py) and starts a new epoch."
    },
    {
      "date": "2026-09-15",
      "change": "cost realism v1.0: config/cost_realism_contract.json declares every cost a funded book would pay and where the paper-live record charges it. scripts/derive_cost_charged_live_curves.py charges the three Alpaca sleeves' filled orders commission, half spread and square-root impact through research's own TransactionCostModel, and the reconstructed daily short book the equity borrow rate; scripts/paper_trading_state.py publishes the result as cost_charged_curve beside live_curve on each sleeve and on the book, with the charges. The broker NAV (live_curve) is unchanged and remains published; no sleeve's sizing, parameters or execution moved, and the live configuration fingerprint is identical.",
      "reason": "The owner's Sharpe target is net of real-life costs and the equity paper-live record charged none (docs/design/COST_REALISM_AUDIT_2026-09-14.md). An accounting repair keeps the original and discloses the correction; the correction is a second published curve, not an edit to the first. Latency, financing, cash yield and FX are not charged and the contract says why and when they will be.",
      "evidence": "config/cost_realism_contract.json; scripts/derive_cost_charged_live_curves.py; artifacts/engineering/cost_charged_live_curves.json (regenerated each tick); data/paper/state.json cost_charged_curve and cost_charges; artifacts/engineering/live_config_fingerprint.json (unchanged); docs/design/OPERATOR_LOG_2026-09.md",
      "contaminates_forward_record": false,
      "note": "An accounting change, not a trading change: what the sleeves do is identical before and after; what is published about it gains a second, cost-charged series. The forward-evidence evaluation reads the cost-charged series from this date and says so in its own output (curve_basis); the epoch does not restart because the returns being evaluated were produced by the same unchanged configuration."
    },
    {
      "date": "2026-09-15",
      "change": "drawdown control v1 ACTIVATED: config/drawdown_control_contract.json activation.live is true and configs/base.yaml risk.book_ladder.source is https. From this date every sleeve multiplies its target gross by the BOOK-level multiplier the publisher derives from the combined book's published marks: half gross at 5.0% below the high-water mark, flat at 10.0%, absorbing until an owner rearm in config/book_ladder_rearms.json. The equity sleeves apply it from the next cycle after this commit; the crypto sleeve applies it from the companion-file rollout of the contract and base.yaml to Frankfurt, whose receipt is recorded in the operator log.",
      "reason": "The owner's 10% maximum-drawdown bound is a bound on the combined book (config/owner_goals.json, restated 2026-09-14). The ladder is derived from that bound (half gross at 5.0%, flat at 10.0%; contract version 1.1), was measured on the current-composition paths by the declared protocol and accepted as the bound mechanism (artifacts/analysis/drawdown_control_v1/result.json: conservative p95 0.1007 and p99 0.1020 with the absorbing ladder, within the pre-declared rule), the consumers were wired default-off and tested (PR #32) and re-derived for v1.1 (PR #39), and the owner decided to activate: 'i give you full permision for the activiations so you can go ahead'.",
      "evidence": "artifacts/engineering/live_config_fingerprint.json; artifacts/engineering/book_drawdown_ladder.json; config/drawdown_control_contract.json activation.decision; docs/design/DRAWDOWN_CONTROL_V1_PROTOCOL.md; docs/design/OPERATOR_LOG_2026-09.md",
      "contaminates_forward_record": true,
      "note": "A trading change: the book is sized differently from this date whenever the book ladder is below 1. The forward-evidence epoch restarts on this date; returns before it are published as a prior epoch and never pooled (forward_evidence_contract rules.configuration_change). Re-pinned on 2026-09-15 from sha256:654432cc to sha256:553aff51: the activation PR also moved BookLadderCfg.source's code default from file to https so it equals configs/base.yaml, and the fingerprint surface reads that setting; the surface declared at activation still said file. The traded configuration is unchanged (every sleeve reads the public artifact, as this entry states); no new epoch."
    }
  ]
}
