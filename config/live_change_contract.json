{
  "schema": "canli.alphac-live-change-contract.v1",
  "purpose": "A change to how the LIVE book trades must be DECLARED before it ships. This file is the declaration. It records the configuration currently in force, and every change to it, with the reason and the evidence. If the measured fingerprint does not match declared_fingerprint, the guard fails and the deploy is blocked.",
  "why": "The forward record is the only evidence that can ever defeat deflation: a pre-registered book run forward is N=1, while any selected backtest must carry the corrected 228-identity search context. That record is the most valuable asset in the project. A live configuration change silently contaminates it, and until this file existed nothing stood in the way of one -- there were 85 checks around admitting a sleeve and none around re-sizing the whole book.",
  "the_incident_this_exists_for": {
    "date": "2026-08-21",
    "what": "cov_halflife_bars (default 720) was changed to cov_halflife_days (default 21). No call site passes that argument, the walk-forwards are regenerated by the daily ticks, and live_cycle.py submits the last leg's weights to the broker -- so the change would have re-sized two broker-executed sleeves on the next tick. The commit said 'nothing was re-run'. Reverted the same day after an adversarial audit.",
    "the_rule_it_produced": "A DEFAULT NOBODY OVERRIDES IS NOT A DEFAULT, IT IS THE PRODUCTION SETTING. Before changing one, grep for who passes it; if nobody does, you are changing the live book."
  },
  "declared_fingerprint": "sha256:fe82c4eeed742289bfcfee5d39856ee2bb8ca9cfe209bcc7e34abff98553706d",
  "declared_surface": {
    "strategy_settings": {
      "allocator": "rank",
      "rebalance_bars": 24,
      "rebalance_anchor": "run",
      "cov_window_bars": 720,
      "cov_halflife_days": null,
      "cov_min_periods": 240,
      "realized_vol_halflife_bars": 240,
      "cost_frac_oneway": 0.001
    },
    "risk_path_settings": {
      "vol_target_ann": 0.15,
      "vol_scale_max": 1.5,
      "gross_max": 1.0,
      "dd_halve_gross": -0.1,
      "dd_flat_halt": -0.15,
      "flat_cooldown_bars": 336,
      "staleness_max_bars": 2,
      "w_max": 0.15
    },
    "book_composition": {
      "sleeves": {
        "alphaforge": 0.25,
        "alphamax": 0.25,
        "managed_futures": 0.25,
        "alphavintage": 0.25
      },
      "strategic_tilt_pct": 0.1,
      "strategic_tilt_mix": {
        "BTC": 0.5,
        "SPY": 0.5
      }
    },
    "book_aggregation_settings": {
      "scheme": "fixed",
      "book_level_vol_target_ann": null,
      "book_level_drawdown_ladder": null,
      "missing_mark_policy": "ZERO_CONTRIBUTION_ON_MISSING_DAILY_SLEEVE_MARK",
      "strategic_overlay_is_book_vol_scaled": false,
      "claim_boundary": "Constituent strategies own their internal sizing. ALPHAC combines their realized returns at the committed fixed-weight schedule and adds the separately disclosed strategic overlay. It does not apply a second book-level volatility target or drawdown ladder."
    }
  },
  "declared_at": "2026-08-21",
  "how_to_change_the_live_book": [
    "1. Measure the change on the LIVE path, not a simulation of it. The overlay halflife was priced against an untruncated EWMA the production estimator is not (artifacts/analysis/live_covariance_memory).",
    "2. Write the reason and the evidence path into change_log below.",
    "3. Re-run scripts/export_live_config_fingerprint.py and update declared_fingerprint.",
    "4. Expect the forward record to be marked at that date. A change is a new specification, and the record before it and after it are not the same experiment."
  ],
  "change_log": [
    {
      "date": "2026-08-21",
      "change": "initial declaration; configuration unchanged",
      "reason": "Establishes the baseline against which future changes are detected. The configuration recorded here is the one the book has been running since the v3 re-baseline on 2026-08-07.",
      "evidence": "artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false
    },
    {
      "date": "2026-08-21",
      "change": "fingerprint COVERAGE extended to the overlay, the drawdown ladder and the per-name clip (backlog A2). The live configuration itself is unchanged.",
      "reason": "The first fingerprint covered BlendStrategy's constructor defaults and the book composition, and claimed to cover 'how the live book trades'. It did not: the overlay's vol target and scale ceiling, the ladder's halve and flatten depths, the halt cooldown, the staleness limit and the inviolable per-name cap are read off Settings rather than passed to the constructor, so all eight sat outside the guard. Each one moves every position in the book. Read through Settings and PortfolioConstraints.from_settings so a config change reaches the fingerprint by the same path it reaches the book.",
      "evidence": "artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false,
      "note": "A coverage change, not a configuration change: every value recorded here is the value that was already running. The fingerprint hash necessarily moves because the surface it hashes is larger."
    },
    {
      "date": "2026-08-23",
      "change": "fingerprint COVERAGE extended to the ALPHAC flagship aggregation layer. The live configuration itself is unchanged.",
      "reason": "The prior surface recorded constituent BlendStrategy risk settings and the flagship composition but did not record how the constituent returns are combined. That omission allowed the constituents' 15% internal volatility target to be described incorrectly as a book-level target. ALPHAC actually compounds the committed fixed-weight sleeve schedule, adds a fixed unscaled strategic overlay, treats a missing daily sleeve mark as zero contribution, and applies no second book-level volatility target or drawdown ladder. These were already the running semantics; this declaration makes them explicit and mutation-guarded.",
      "evidence": "artifacts/engineering/live_config_fingerprint.json",
      "contaminates_forward_record": false,
      "note": "A coverage and claim-scope correction, not a trading change. The fingerprint moves because the previously omitted aggregation layer is now hashed."
    }
  ]
}
