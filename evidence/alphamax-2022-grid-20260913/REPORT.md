# AlphaMax active historical session grid: complete

The333retained active-window names have236,865expected active XNYS session observations over2020–2022. Every expected timestamp is present. No duplicate timestamps or off-calendar observations were found. The underlying calendar has756sessions in this window. Membership intervals, not the whole375-name configuration, define the expected active observations.

This establishes date coverage only. It does not certify prices, source publication time, historical universe lineage, corporate actions, borrow availability or executable liquidity. Nonzero quality flags are absent in the inspected records; that is not independent quality certification. Source hashes and per-instrument coverage are retained. The audit script passes Ruff and computes no strategy signals or returns.

Next: use the existing corporate-action repair route after checking its scope and the actual strategy warmup/input dependencies. No price gap acquisition is needed for the audited active sessions; do not manufacture an extra missing-data gate for five configured identifiers absent from retained membership.
