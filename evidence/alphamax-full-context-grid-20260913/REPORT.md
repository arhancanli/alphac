# AlphaMax full-context price coverage and dependency tests

The unchanged baseline's declared shared context spans2010-12-27through2022. Across333run-window names,845,172sessions expected within latest retained listing boundaries are present, with zero missing sessions or duplicate timestamps. Listing dates are retrospective metadata assumptions, not contemporaneous observations. The2520-session volatility headroom is retained; no warmup was shortened.

Four additional bars occur exactly on retained delisting date labels:ABMD2022-12-21,AVLR2022-10-18,CLR2022-11-21,TWTR2022-10-27. Each contains nonzero volume. These are same-date endpoint-convention conflicts, not long missing terminal intervals. Source rows and metadata are retained separately. A replay must state its final recorded session/administrative-close convention; these bars alone do not establish actual acquisition settlement time or cash proceeds. No source row or loss was deleted.

Five synthetic tests pass using the existing adjusted_close and252/21momentum kernels. Perturbing a2019-12-11split ratio across reverse/forward/extreme positive values changes earlier momentum but leaves every output from2020-12-30onward exactly equal. A split inside the endpoint window produces a change, verifying that the test is not ignoring all actions. Combined with the source dependency trace, this supports exclusion of pre2019-12-30action metadata from the scoped momentum calculation: those events only scale price rows earlier than any output endpoint. Raw-close volatility does not read action metadata. Execution begins much later and likewise does not apply those old events. This is a bounded dependency argument, not a global validation of the corrected lake or proof of all PIT requirements.

The existing corporate-action repair remains the input route. Its global historical failures stay recorded. A scoped snapshot can retain full raw-price headroom while selecting only the action dates needed for the proposed run; must bind this filter explicitly and preserve excluded source evidence. No stock may be removed based on its returns or the unresolved historical split list.

Baseline policy to freeze next: latest metadata labeled modeled; corrected per-share dividends and applicable splits; unchanged raw-close sigma, raw-open forward labels,252-session training/63-session test legs, original rank allocation and costs. The raw-open label and raw-close volatility conventions are existing model limitations, not corrected merely by repairing dividend cash. An adjusted-volatility/holding-label challenger must be separately preregistered. Next assemble the scoped snapshot and freeze the baseline runner, then measure. No signals or strategy returns computed; union284.
