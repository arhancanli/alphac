# AlphaMax recovered reference evidence

All 12 available leg equity files concatenate exactly to the preserved 729-row reference curve. All five originally sealed root files match their hashes. The current 113-file tree is copied and indexed in `inventory.json`; it includes orders, fills, positions, equity and metadata for each leg. Leg boundaries, final-equity metadata and cash continuity match.

This closes the current missing-reference-leg gap. It does not establish historical capture time, original raw-input recovery or exact fresh-input strategy reproduction. The earlier mismatched replay's temporary workspace was destroyed. No matching replay equity was found in the bounded artifact search. Root mismatch cause remains undetermined, and the original failed reproduction receipt is preserved.

No strategy rerun or return trial occurred; union remains 238. The next diagnostic replay must retain its full output tree and pass accounting preflight before execution. The recovered reference must remain immutable.

See `result.json` for per-leg counts and explicit limits, and the sibling AlphaTrend observation readiness report for the combined handoff.
