# Equity cooldown research candidate

The shared default is documented as336hours (14days on an hourly market), but the ladder counts updates. The retained equity reference therefore waits336sessions after its drawdown halt. Existing evidence shows336flat sessions and one rearm. This is a documented time-scale mismatch, not proof that a shorter pause earns higher returns.

One candidate sets the equity-only research configuration to10sessions (roughly two trading weeks, not exact14calendar days). The prior336-session reference, crypto/Trend configurations, drawdown thresholds, HWM reset behavior, allocations and histories remain preserved. A shorter pause can permit repeated losses; combined whole-history/annual drawdowns must include losses across every reset.

The frozen design requires normal and doubled-cost candidate paths, then combined comparison with the unchanged source components. No duration grid or threshold adjustment is permitted in this phase. A failed combined gate stops this family; a pass requires a separately preregistered2022 check before any promotion. All inspected-period results remain development evidence, not untouched validation or qualification.
