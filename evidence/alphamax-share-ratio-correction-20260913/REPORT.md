# AlphaMax split-unit mismatch: synthetic proof and scoped correction

Stored action ratios are split_to/split_from (new shares per old share). The ledger multiplies quantity and divides per-share entry price by this ratio. The reader and FeatureContext forward the stored ratio unchanged. The feature adjusted_close helper instead multiplies earlier prices by the ratio. Existing factor tests supply reciprocal price factors (0.5 for a2-for-1 split), explaining why those tests miss the integration mismatch.

A synthetic4-for-1 split in a smooth price series generates an artificial legacy log-return of0.001-2*log(4), approximately-2.771589. Correct share-unit conversion preserves the economic0.001 log-return. Raw-price EWMA sigma also spikes. Sigma multiplies standardized alpha in Grinold expected-return sizing, so this is not merely a reporting column. Both momentum and expected-return estimates may be affected; no historical performance effect measured here.

New opt-in helper converts share ratio to reciprocal price factor, then uses the existing split-only math; volatility uses that adjusted series with unchanged168span. Eight synthetic tests cover ratios2,4,20,0.125; economic returns and momentum conservation; sigma conservation; future-event prefix invariance; latepublication rejection; no-action parity; gaps and input immutability. Existing frozen engines, signals, momentum feature and trial inputs remain untouched.

All20 scoped split metadata records in the retained2020–2022 candidate lake have modeled available_at by ex_date+1day. This is metadata applicability, not historically verified knowledge. Older actions outside the previously frozen scope remain absent; their raw-volatility contribution is not silently declared corrected. Raw-open holding labels remain a separate known limitation.

Next freeze6arms: originalbaseline primary/doubledcost exact replay controls; share-ratio-corrected momentum only primary/doubledcost; correctedmomentum+correctedsigma primary/doubledcost. Keep168span,252momentum/21skip,ranktop30,horizon21,rebalance63,calendar/tail policy,capital and raw-open labels unchanged. Require baseline curve parity against286/287 and preserve all failed identities. Fixed correction tests precede any historical feature/return computation. This is one necessary semantic correction comparison, not a parameter sweep; no new sleeve qualification or return claim.
