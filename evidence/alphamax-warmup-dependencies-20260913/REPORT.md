# AlphaMax baseline dependencies and metadata resolved

For a proposed first test session2021-12-30 (providing a prior daily observation for2022), unchanged252-session training begins2020-12-30. SignalService requests momentum253sessions, raw-close EWMA volatility2520sessions and open price1session. FeatureEngine therefore loads its shared context from2010-12-27. The previously completed2020–2022price grid is insufficient to certify that entire declared context; the warmup must not be silently shortened.

The momentum-local253-session boundary is2019-12-30. Eight retained split-failure records intersect the shared context and333run-window names; none intersects the momentum-local window. The shared loader reads the wider action window, so local dates alone do not grant blanket corporate-action clearance. Trace numerical dependency separately from loader reachability. The volatility feature directly uses raw closes and ignores action metadata; raw split jumps may affect its model estimates. Preserve this baseline and treat any split-adjusted equity-volatility alternative as a distinct preregistered experiment, not an undisclosed replay fix.

There are666retained metadata versions for333names and no missing instrument record. Zero versions were recorded by the proposed training start. All333latest versions change maker/taker fees from0to1bps and replace placeholder listing dates;14also change delisting dates. Both versions and field differences are retained. These records can support a labeled modeled historical reconstruction, not a point-in-time metadata claim or arbitrary choice of whichever version improves performance.

No strategy signals, historical return paths or trial identities were computed. Original baselines and metadata were not changed. Next: audit declared2010–2022price availability with genuine listing boundaries and derive the older-action dependency restriction. Then freeze corrected-action source, metadata/fee policy and full2022replay protocol. No broad qualification or exact legacy reproduction is established.
