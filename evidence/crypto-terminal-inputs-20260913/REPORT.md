# Crypto historical comparison inputs and design

Copied and hash-verified375 price, funding and membership partitions for the fixed58-instrument universe:43,089,948bytes. Price/funding span2020–2022; membership partitions retain original intervals. Four repaired2022partitions contain the prior360 recovered bars. The original four partitions are separately copied and verified, allowing matched original/repaired controls without dependence on changing production data. Only58 instrument metadata versions were exported from the operational database; no account or authentication tables were copied. Current observation/version timestamps remain intact and do not certify historical applicability.

COMPARISON_DESIGN.json fixes seven arms: original close-price funding control, repaired close control, repaired open control, conditional lower and upper settlement scenarios, and doubled-cost versions of both settlement scenarios. Both endpoints must be reported; neither is a certified actual settlement price. Terminal5bp fee and10bp stress are explicit models. This is a correction-sensitivity experiment, not a tuned alpha candidate.

The first diagnostic retains the original first two walk-forward legs,6048-hour training,1512-hour tests, weekly rebalancing and preceding-leg risk/cash state. It does not substitute a cold-start May-only leg or claim full-year2022 coverage. This bounded comparison precedes the full-year extension. Historical input repair, funding valuation and terminal settlement are separate comparisons. The existing walk-forward runner restarts positions each leg, so termination eligibility must remain enforced in later legs as well.

All375 primary snapshot hashes were independently rechecked; four original overrides also match their recorded hashes. Snapshot preparation script passes Ruff. No signals, returns, reservation, or new trial identity were computed. Union273. The design is fixed, but the runner is not yet sealed: next implement explicit funding-policy selection and persistent terminal eligibility in the walk-forward adapter, test their integration, then bind the runner/environment and register before computation. Historical metadata, notice timing, funding marks and exact settlement remain modeled or unverified and must be disclosed in results.
