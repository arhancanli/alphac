# Versioned earnings runner fixes cost splits and stress gate

Previous phase made progress by implementing the runner. Review confirmed LakeCostInputs estimates volatility from raw-price log returns, which count stock splits as economic moves. Added isolated EarningsSplitCostInputs: add log(split_to/split_from) only to the affected ex-session return before the same240-session-halflife EWMA. Uses complete frozen action history including cost warmup, not only evaluation-period actions. ADV and raw execution prices are unchanged. Retrospective action semantics remain modeled; no prospective action-availability claim.

Four focused tests pass. A synthetic lake/provider test proves pre-split volatility and all ADV entries unchanged while removing the artificial four-for-one volatility jump. Other tests preserve true economic movement and future-truncation invariance and reject invalid/duplicate split events. The first provider fixture supplied40observations against240required, yielding NaN; extended only synthetic history beyond warmup, retaining the failure output. No estimator warmup threshold changed.

Added stress progression guard requiring named accounted/packet-closed status, exact audit and trial-packet bindings, valid hashes and a passing independent audit. Full packet schema is still the closer's responsibility; a nonempty packet alone is not admission evidence. The guard tests reject missing/empty closure and modified evidence. Versioned runner v2 records timestamped failures and preserves v1. It injects the corrected cost provider and requires the strengthened normal closure before stress.

v2 preparation succeeds with9,312bound files at artifacts/analysis/earnings_change_20260913_v2. No real input book was scored, no return/reservation consumed. Original prepared v1 remains untouched. Compilation/import and component tests are not full end-to-end runner validation; canonical trial config/reservation preflight remains next, followed by normal replay if accepted. All output/ledger accounting and packet closure must precede stress. No new formula/rank/weight choices; union329/reference316–317 unchanged. Goal active, no running jobs.
