# Monthly earnings strategy: engine timing and exit integration

Previous phase made progress by implementing the fixed accounting score. Added isolated scripts/earnings_monthly_strategy.py implementing the engine Strategy interface. Every callback maps its UTC-midnight equity label to the preceding source session's physical close and the current source session's physical open. Monthly rankings use that backward physical close for score and membership providers; the first session of each month triggers the rank. No historical earnings score or return trial ran.

Two actual synthetic EventDrivenBacktester runs with40equities verify the monthly route. Source November29 2024 closes105 at18:00UTC; next month December2 opens120. All40orders execute at December2's raw120 open with modeled cost shifts, not prior100open/105close. The provider is called exactly once at November29 18:00UTC. Fill timestamps remain engine session labels, not physical exchange-open timestamps; the route records both clocks. A second run withdraws one short's eligibility at December2 physical close and verifies exactly one additional exit on December3,41fills total.

This exit test initially produced80fills: re-emitting the entire target book on one membership exit unintentionally rebalanced39unaffected holdings. Fixed the wrapper to emit only banned-name zeros unless monthly allocation or a risk reduction is due. The failed output tests_final.txt is retained, despite its provisional filename. Earlier fixture error used the full instrument ID as base symbol; corrected to the actual base, retaining tests_attempt1.txt. tests_verified.txt is the authoritative terminal19-pass result.

Every-bar DrawdownLadder uses10%half,15%flat,10sessioncooldown. A synthetic equity path100000->89000->84000 between monthly decisions verifies gross1->.5->0 and only one score-provider call. Recovery never increases targets between monthly rebalances. Member/shortability checks refresh each bar and force explicit exits. Synthetic target gross is not a realized drawdown guarantee. The engine's source prices, actions, costs and admission requirements remain independent.

Tests now total19: filing inputs, fixed formula/selection/calendar, risk transitions, actual40-name next-month open execution and isolated membership exit. Actual fills were tested with EventDrivenBacktester and synthetic static cost inputs; this does not certify historical payable-dividend settlement, borrow availability, terminal recovery, ADV capacity or the final cost-stress runner. No historical capital or live orders were touched.

NEXT assemble an isolated continuous earnings replay runner using the frozen equity lake and audited SF1 mapping, corrected corporate-action/borrow/payable handling, exact normal/stress costs and durable outputs. The existing AlphaMax engine path is the raw-open timing reference; confirm any payable extension's identical timing before use. Freeze complete source/config/gate manifests and reserve before loading historical values into score calculation. Candidate earnings formula/monthly20+20 design unchanged, no adjacent variants. No need to repeat source searches, coverage scans or basic calendar tests. Reference316/317/union329 unchanged; no performance improvement or new qualified sleeve. Goal active, no live jobs.
