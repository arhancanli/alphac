# Payable engine timing and archive provider ready

Previous phase made progress on monthly engine integration. This phase verifies the same physical-close/month-start-open mapping with PayableEquityBacktester. Three synthetic tests pass: every-bar risk reductions,40monthly raw-open fills, and isolated eligibility exit producing41total fills. Payment lists are empty in these tests; this proves timing/exit parity, not dividend settlement coverage.

Added scripts/earnings_archive_book.py: hash-verified SF1 and mapping inputs, exact raw ticker/stable IDs, explicit endpoint exclusion, decimal accounting/FX validation, duplicate/currency conflict rejection, invalid-row counters and a backward score provider. Construction does not calculate scores. Historical scoring is disabled until the runner explicitly confirms validated reservation. This Boolean guard is an internal lifecycle check, not a substitute for the canonical validate_reservation call and manifest verification. Tests use synthetic SF1 only and prove literal NA handling, endpoint exclusion, formula wiring, pre-reservation rejection and input hash rejection. Four payable/provider tests pass together.

No real historical input book was scored or enabled. No trial reservation, return, new sleeve or metric gain. Existing fixed formula/ranks/risk unchanged. Costs remain to be bound using commission1bp,halfspread3bp,borrow50bp annually,impact1,latency2bp normal and2xstress; actual metadata-dependent fee handling and capacity need runner checks.

NEXT complete the continuous runner's source preparation: typed frozen corporate actions plus explicit modeled dividend-pay schedule (historical cash-pay dates are not supplied by the frozen action table), frozen instrument/terminal metadata and membership provider, exact costs and risk bindings. Reuse existing corrected payable/action helpers where applicable; if using ex+nextsession payment, label it modeled and verify parity with the reference convention. Bind runner/config/manifests and validate/reserve through the canonical trial system before enabling historical scores. Do not treat tested empty-payment timing as completed accounting verification. No repeated source/coverage scans. Union329/reference316–317 unchanged; overall goal active, no running jobs.
