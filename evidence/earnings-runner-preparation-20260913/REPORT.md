# Continuous earnings runner implemented and inputs frozen

scripts/run_earnings_change_replay.py now implements prepare, normal and stress commands. Preparation completed successfully and binds9,310 files in artifacts/analysis/earnings_change_20260913/input_manifest.json, including the frozen lake, SF1, mapping, actions, execution inputs, Python source and environment lock. No historical score, backtest or reservation was executed. Previous phase made progress on action/cost preparation; this phase adds the executable runner rather than repeating source discovery.

The run path validates source hashes and protocol, writes a canonical reservation and validates it before enabling earnings scores. It assembles modeled metadata, physical-session routes, membership/lifecycle eligibility, complete typed retrospective actions/payment schedule, and PayableEquityBacktester with frozen normal/2xstress costs. It runs one continuous100000cash book, persists engine output/strategy audit, records the experiment and writes a measurement marker requiring independent audit. Stress refuses to begin until normal closure exists. The closure-content validation should be tightened during final review; file existence alone is not accounting proof.

START isDecember29 2022 source-session warm start. END isJune3 2026exclusive engine label, retainingJune1sourceclose atJune2label. Aggregated standalone/session metrics include warm-start marks and must be separately aligned to the explicitly frozenJan1 2023–June1 2026 fullUTCcombined evaluation during independent audit. Pending dividend NAV is the payable ledger's equity, not settledcash only.

Compile/import and prepare checks pass. Existing synthetic feature/engine/payable tests provide component evidence, but this newly assembled runner has not yet completed an end-to-end smoke/preflight. Do not describe compilation as validation of historical execution. Final review must check canonical reservation schema/config compatibility, terminal eligibility, stress closure content and durable failure recording. The inherited LakeCostInputs raw-price cost sigma can react to splits; this limitation is explicitly in the protocol and requires a bounded check before the return trial. Do not silently change a frozen implementation: any needed correction gets a versioned runner/manifest rather than overwriting this prepared packet.

NEXT finish bounded runner preflight and the above identified concerns, then run the normal arm under its validated reservation. Preserve failure/error artifacts, close independent raw-fill/borrow/dividend/position/equity accounting before stress, then evaluate the fixed combined candidate. No formula/rank/weight sweep. No new qualified sleeve or metric gain; union329/reference316–317 unchanged. Goal active, no live jobs.
