# Fundamental release-order regression: counterexample confirmed, no cohort exposure found

The prior phase produced source-feasibility progress. This phase reviewed the implemented fundamental family before proposing another forecast. Existing quality/value/investment trials remain counted. The glassbox export also discloses a killed PEAD campaign; that disclosure prevents treating earnings drift as a fresh untested idea, but this phase does not independently replicate its advertised numbers.

A concrete correctness gap was reproduced in the retained period-ordered quarterly sum. In the adversarial four-quarter example, Q3 is released at time10 while Q4 is released at time5. Computing TTM on the full period-ordered frame yields100 at time5; computing with only available rows yieldsmissing. The derived row's own availability does not certify every component's availability. This disproves an unconditional availability-prefix guarantee for this reducer; it does not prove actual strategy leakage.

The bounded real-source audit covers358 retained equity IDs,8240 local parquet partitions and30941 rows throughJune1 2026. NOK has no fundamentals partition. It finds ZERO four-row windows where a component release follows the endpoint release and ZERO duplicate-period issuers. Source hashes were checked before and after the scan. Thus this audit finds no observed exposure to the specific ordering counterexample in this cohort. Do not rerun killed fundamental strategies or claim a repaired historical Sharpe on this evidence. Broader source vintage, fiscal continuity, annual/YTD normalization and market-cap share-basis questions are not certified here.

An isolated research reducer, scripts/fundamental_release_order.py, now processes complete release-time batches; keeps the latest known fiscal endpoint; requires four consecutive finite quarterly flows; rejects duplicate/revised periods, annual labels and inconsistent timing; and deliberately retains missing output events. Nine tests pass, including every release prefix, shuffled same-time input, missing quarters, nulls, cross-issuer isolation and rejection cases. It does not reconstruct revisions, infer original filing timestamps, normalize YTD flows or certify sources. It is staged for future reviewed consumers; it is NOT wired into the frozen production feature registry or historical experiments. Existing source bindings and baselines are preserved.

The first local audit attempt failed on a timezone-naive cutoff before producing results. Original audit_source.py and source_audit_v1_failure.json are retained. audit_source_v2.py uses an explicit UTC cutoff and completed successfully. No returns, trial identities or qualified sleeves were added. Combined reference remains0.742413 normal/0.418865 stressed excessSharpe. All launched jobs terminal.

Phase boundary: regression evidence and a tested isolated correction are complete. Historical impact of this specific issue was not found, so stop this maintenance lane here. Next work must return to a materially distinct economic forecast and avoid relabeling failed PEAD, fundamental levels, own-price momentum, or source-blocked industry propagation. Goal remains active; no qualification or performance-improvement claim.
