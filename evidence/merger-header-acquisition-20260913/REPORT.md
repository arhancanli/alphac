# Merger confirmation headers — September 13

Acquired all395 distinct header files for the sealed400-row index sample. The first source probe is retained and reused with its hash verified;394 subsequent GETs completed successfully. No retry, replacement accession, skipped request or network failure. Receipts retain source URLs, capture times, sizes and SHA256 hashes. Requests used at most two concurrent connections with a0.35-second minimum start gap.

Every header matches its selected accession and form. All395 raw14-digit acceptance clocks parse as valid dates/times; all400 index CIKs match recorded entity roles. Mapping counts:200 FILER,189 SUBJECT-COMPANY+FILED-BY,5 FILED-BY-only,6 SUBJECT-COMPANY-only. The10 rows sharing5 accessions retain separate mappings. These role counts describe filings, not independent deals or target eligibility.

The five duplicated index accessions now have source-bound role mappings, but transaction identity is not resolved. A SUBJECT-COMPANY role must not be silently treated as the acquisition target. No human eligibility labels, transaction conclusions, consideration extraction or return computations were generated. Filing bodies and primary-document identity remain absent.

Raw acceptance clocks are preserved exactly. Zero UTC acceptance clocks are certified: timestamp convention verification remains required before conversion or trading-time use. The source capture time is distinct from the historical acceptance value, and an anchor's acceptance time is not automatically the earliest binding announcement time.

The mechanical parser has two passing tests covering separate entity roles, no invented target/timezone, and rejection of duplicated or invalid clocks. Both scripts and tests pass Ruff; the raw clock syntax check intentionally suppresses the naive-datetime lint because it assigns no timezone. Full-corpus identity checks and receipt hashes pass. closure.json binds the retained outputs and code.

Next: acquire filing index/document manifests and the exact primary documents for these395 filings; retain all400 mappings and five shared-accession ambiguities. Verify historical clock semantics before converting timestamps. Assemble independent review packets without machine eligibility labels or outcome/pricing contamination. Do not resample to replace ambiguous rows. Both-stratum independent review and later return preregistration remain necessary.

Union256 is unchanged. No new qualified sleeve, performance result, production change or admission. The current portfolio proxy excess Sharpe remains0.909 for the confirmation candidate; collecting headers does not improve it.
