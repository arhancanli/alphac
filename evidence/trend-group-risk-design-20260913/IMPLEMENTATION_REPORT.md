# Group-risk allocator and strategy integration

Implemented isolated `scripts/trend_group_risk_allocator.py` and `scripts/trend_group_risk_strategy.py`. No production defaults or frozen parent files changed. Fixed four-group mapping and forecast signs are retained. Each active group receives unit estimated stand-alone volatility before the existing global gross and per-name caps. Cross-group covariance still influences downstream total portfolio volatility; this sizing stage does not equalize final marginal risk.

16 focused synthetic tests pass. Coverage: unequal group cardinality, within-group correlation, cross-group covariance effect on total risk, ID permutation and covariance-scale invariance, shortability and zero signals, missing groups, single-group parity with original capped allocator, final caps, duplicate/unknown identities, asymmetric/nonpositive inputs and zero-variance hedged groups. Real strategy-method tests exercise covariance estimation/filtering: a constant SHY column is removed before IDs reach sizing and its carried holding receives an explicit zero target; direction reversal still needs two confirmed rebalances; cold start does not advance confirmation memory.

The isolated strategy copies the frozen paired rebalance method with exactly one allocator-call substitution that passes final filtered ordered IDs. Other covariance, raw-mark, cost-array, volatility-overlay, cash-retention, ladder and held-name exit logic is unchanged. Original confirmation dispatch is inherited via ordinary Python multiple-inheritance MRO; no global monkeypatch or runtime source rewriting. Source hash and copied-method provenance retained. The synthetic tests use synthetic panels, not historical signals/returns, and do not establish engine fill/accounting or future source availability.

Known limitations before historical execution: runner binding must include the isolated scripts and exact covariance/input source manifests; group diagnostics must be persisted per rebalance; full-engine synthetic timing/fill smoke and canonical reservation remain pending. Invalid nonpositive active group variance raises an error rather than substituting a new optimizer or ridge; near-singular positive covariance can concentrate targets subject to retained caps. No altered covariance window/ridge/budget/classification search. All existing source-vintage/payment/borrow/collateral limitations carry forward.

Next: bind one normal/stress main-period replay to the unchanged BIL reference and fixed allocator, retain per-rebalance diagnostics, freeze standalone/combined/main/2022 gates before any historical candidate computation, and validate canonical reservation. Complete normal accounting/packet before another return identity. This is an existing Trend construction hypothesis, not a new economically distinct sleeve. Union334 and provisional references331–334 unchanged; no historical returns computed.
